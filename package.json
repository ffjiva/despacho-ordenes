{
  "scripts": {
    "emulators": "firebase emulators:start --only firestore,auth",
    "seed:emulator": "node scripts/emulator/seed.js",
    "test:e2e": "node scripts/emulator/smoke.mjs"
  },
  "dependencies": {
    "firebase-admin": "^14.1.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "playwright": "1.62.0"
  }
}
